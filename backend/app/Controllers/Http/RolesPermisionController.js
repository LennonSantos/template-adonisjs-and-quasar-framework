'use strict'

/**
 * Resourceful controller for interacting with rolespermisions
 */
class RolesPermisionController {
  /**
   * Show a list of all rolespermisions.
   * GET rolespermisions
   */
  async index ({ request, response, view }) {
  }

  /**
   * Create/save a new rolespermision.
   * POST rolespermisions
   */
  async store ({ request, response }) {
  }

  /**
   * Display a single rolespermision.
   * GET rolespermisions/:id
   */
  async show ({ params, request, response, view }) {
  }

  /**
   * Update rolespermision details.
   * PUT or PATCH rolespermisions/:id
   */
  async update ({ params, request, response }) {
  }

  /**
   * Delete a rolespermision with id.
   * DELETE rolespermisions/:id
   */
  async destroy ({ params, request, response }) {
  }
}

module.exports = RolesPermisionController
